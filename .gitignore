# Build artifacts
archon
archon.exe
archon-test
# Cross-platform release builds (archon-linux-amd64, archon-darwin-arm64, ...)
archon-*-amd64
archon-*-arm64

# Local directories
.archon/
.claude/

# Dependency directories
homebrew/

# Note: openspec/specs/ (synced main specs) and openspec/changes/archive/
# (archived SDD changes) are intentionally versioned — they are the audit trail.
