# openspec/config.yaml — SDD Project Configuration
# Auto-generated by sdd-init on 2026-06-10
schema: spec-driven

context: |
  Tech stack: Concept / Framework — AI Agent Orchestration Harness for developers
  Language: None detected (conceptual/framework project)
  Architecture: Spec → Gherkin → TDD Implementation → Judge (mutation testing) → Verify pipeline
  Testing: No test runner implemented yet — strict TDD disabled
  Patterns: SDD workflow, Gentle AI skills orchestration, agent-interaction harness
  Repository: archon-ai — personal project by alexcasdev

rules:
  proposal:
    - Include rollback plan for risky changes
    - Define scope boundaries clearly for AI agent orchestration features
    - State which agents/skills are affected by the proposal
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Specify agent interaction contracts explicitly
  design:
    - Include sequence diagrams for complex agent flows
    - Document architecture decisions with rationale
    - Consider agent interaction boundaries, state management, and skill resolution
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Prefer small, verifiable increments for agent behavior
  apply:
    - Follow existing patterns in the codebase
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: >
    No test runner or test framework detected in the project tree.
    This is a conceptual/framework project with no code files yet.
    Strict TDD will be enabled automatically when a test runner is detected
    (e.g. vitest, pytest, go test) after code implementation begins.
    Run `sdd init` again to re-evaluate when tests are added.
  runner: null
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null
